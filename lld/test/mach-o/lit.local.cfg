if 'lld-macho' not in config.available_features:
  config.unsupported = True

# mach-o test cases encode input files in yaml and use .yaml extension
config.suffixes = ['.yaml']
config.excludes = ['Inputs']
